# Experiments

Each experiment should include config, logs, and a short write-up.

## Suggested Structure
- `configs/`: YAML or JSON configs
- `runs/`: raw logs and checkpoints (gitignored if large)
- `results/`: tables, plots, and analysis notes
